<UserControl x:Class="OracleByFPCLtd.UI.Panels.ProjectDataPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock x:Name="ProjectDataHeaderTextControl" Text="Project Data" FontWeight="SemiBold" Margin="0,0,0,8" />
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,6">
      <Button x:Name="UploadProjectButtonControl" Content="Upload Project (.apex)" Width="180" Height="28" />
      <ComboBox x:Name="RecentProjectComboBoxControl"
                Width="240"
                Height="24"
                Margin="10,0,0,0"
                DisplayMemberPath="FileName" />
      <Button x:Name="ProjectPreviewButtonControl"
              Content="Show"
              Width="70"
              Height="28"
              Margin="10,0,0,0"
              IsEnabled="False" />
    </StackPanel>
    <StackPanel Grid.Row="2" Orientation="Horizontal">
      <Button x:Name="UploadAdditionalInfoButtonControl" Content="Upload Additional Info (.xlsx)" Width="180" Height="28" />
      <TextBlock x:Name="AdditionalInfoFileNameTextControl" Text="No additional info" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis" />
    </StackPanel>
  </Grid>
</UserControl>
